# FinRA Configuration
# ====================
# Copy this file to .env and fill in your values

# Required: OpenAI API Key (ONLY required API key!)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: LLM Model Configuration
# Planner and Summarizer use gpt-4o-mini by default (configurable)
LLM_MODEL=gpt-4o-mini
LLM_PROVIDER=openai

# Alternative models you can use:
# LLM_MODEL=gpt-4o          # More capable, higher cost
# LLM_MODEL=gpt-4-turbo     # Fast GPT-4
# LLM_MODEL=gpt-3.5-turbo   # Cheaper, less capable

# ============================================
# Production Settings
# ============================================

# Environment (development/production)
ENV=development

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Rate Limiting & Concurrency
MAX_CONCURRENT=5           # Max parallel requests (reduce if getting blocked)
RATE_LIMIT_DELAY=1.0       # Seconds between requests to same source
REQUEST_TIMEOUT=30         # Request timeout in seconds
MAX_RETRIES=3              # Max retry attempts with exponential backoff

# For production, use more conservative settings:
# MAX_CONCURRENT=3
# RATE_LIMIT_DELAY=2.0
